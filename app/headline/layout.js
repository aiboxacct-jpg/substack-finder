export const metadata = {
  title: 'Headline Analyzer — Stack Tools',
  description:
    'Score your Substack headline on clarity, curiosity, specificity and length, then get five stronger versions.',
};

export default function HeadlineLayout({ children }) {
  return children;
}
